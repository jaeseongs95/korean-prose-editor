# 무효 처리한 run 3

두 번째 맹검 시도의 run 3 selection이 입력의 opaque case ID를 보존하지 않고 새 UUID로 바꿨다. 편집자가 같은 잘못된 ID를 이어받아 자체 순서 검사에서는 통과했지만, 원본 입력과 결속하는 결정적 검사에서 첫 사례부터 불일치가 확인됐다.

이 실행의 selection과 candidate를 그대로 보존하되 평가나 릴리스 지표에는 사용하지 않는다. 원문, 정답, 합격선과 구현 후보는 바꾸지 않고 새 역할 행위자로 run 3만 처음부터 다시 수행한다.
