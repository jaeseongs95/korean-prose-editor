# 무효 처리한 run 1

세 번째 시도의 run 1 selection은 JSONL 레코드의 `actorId`와 `selection-meta.json`의 `actorId`를 서로 다르게 기록했다. 뒤 단계의 자체 검사만으로는 이를 발견하지 못했지만 최종 집계기의 역할 결속 검사에서 130건 전체가 불일치로 판정됐다.

역할 증거 누락을 결과 실패와 구분해 원시 산출물을 보존한다. 이 실행은 지표에 사용하지 않으며 원문·정답·합격선·구현 후보를 바꾸지 않고 run 1 전체 역할 체인만 새 UUID로 다시 수행한다.
