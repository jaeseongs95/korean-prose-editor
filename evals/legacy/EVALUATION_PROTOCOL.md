# 의미 보존 평가 규약

## 판정 기준과 절차

이 규약은 1.0.1 후속 원인 비교 실험의 출력을 보기 전에 고정했다. 문장을 정답 문자열과 일치시키지 않고 아래 의미 조건을 대조한다. 실험 이후 기준을 바꾸면 기존 결과를 새 기준으로 몰래 재분류하지 않고 이유와 영향을 기록한다.

1. 입력별 사실·주장·행위·조건·양태·격식과 보호 문자열을 먼저 확정한다.
   검증 사례가 `request`나 별도 context와 `input`을 구분하면, 전자는 편집 방법과 판정에 쓰는 작업 지시이지 결과 본문에 되풀이할 사실이 아니다. 의미 보존은 실제 편집 대상인 `input` 본문을 기준으로 판정하되 `request`의 보호·출력 조건도 함께 지켜야 한다.
2. 실행자는 평가 기준·정답·과거 결과를 보지 않고 지침과 원문만 받는다.
3. 출력은 의미 보존, 자연스러움, 요청 준수로 나누어 평가한다. 자연스러워도 의미가 바뀌면 합격이 아니다. 원문 유지가 적절한 사례는 변경하지 않아도 합격이다. 명백한 군더더기를 그대로 남기는 무편집을 모든 사례의 해결책으로 인정하지 않는다.
4. 의미 보존은 원문의 주장 중 출력에서 사라진 것과 출력에만 생긴 것을 대조한다. 표현의 길이나 문장 수는 점수가 아니다.
5. 확실한 의미 변화, 해석이 갈리는 차이, 단순 선호를 구별한다. ‘중대한→중요한’ 같은 정도 표현 차이는 문맥에 따른 유보 사항으로 기록하며 그 단어 차이만으로 실패를 확정하지 않는다. 명시된 의무·정의·핵심·결정 행위의 소실은 아래 사전 조건에 따라 판단한다.
6. 최종 검증은 실행과 감사를 다른 에이전트에 맡긴다. 감사자에게 모델 이름·수정 내역·실행자의 자기평가를 제공하지 않는다. 메인이 근거를 대조해 최종 책임을 진다.

## 원인 비교 사례의 고정 조건

| ID | 보존해야 할 의미 | 허용되는 편집 | 실패 조건 |
| --- | --- | --- | --- |
| C01 | 담당자·마감일 공유가 협업의 본질/핵심이라는 판단, 개인에게 개편이 큰 전환점이라는 평가, 화면 수가 아닌 최초 의견 반영이라는 이유, 일반화 유보 | ‘결국’ 제거, 본질을 동등한 핵심 판단으로 표현 | 설명을 공유 의무·권고로 바꾸거나 개인 평가·이유·유보를 없앰 |
| C02 | 되짚기가 기록의 본질/핵심이라는 주장, 검토자 메모의 선택 가능·비의무, 외부 전송 전 보안팀의 승인 의무, 해라체 | 중심 주장이 유지되는 간결화 | 본질 주장을 여러 용도 중 하나로 바꿈, 선택/의무/선행 조건 변화, 무단 격식 전환 |
| C03 | 운영팀이 오류 3건을 2026년 9월 7일 확인, 로그 보관, 다음 점검일을 9월 10일로 정한 행위 | 샘플의 짧은 호흡·해라체 적용, 날짜 결정의 동등한 표현 | 샘플의 1인칭 전이, 날짜·수치 변경, 일정을 정한 행위를 단순 날짜 상태로만 축약 |
| C04 | 오류 재발 방지가 속도보다 중요하다는 비교·평가, 운영팀의 주간 점검 권고, 야간 점검은 의무화하지 않은 사실 | 실제 대비를 유지하는 간결화 | 비교를 일반적인 행동 지시로 바꾸거나 권고·의무 부정·행위자 손실 |
| C05 | 민원 접수서 작성 지원, 입력 주소에 따른 관할 기관 탐색과 접수서 표시 | 근거 없는 홍보성 수식 제거, 자연스러운 문장 분리·병합 | 새 기능 추가/기존 기능 누락, 주어·서술어가 어색한 병합. 의미 보존만을 위해 원문의 과장 전체를 그대로 남기면 문체 개선 미달 |
| C06 | A는 항상 로그 저장, B는 설정에 따른 가능성, 운영팀 대상 매주 검토 권고, 감사 대상의 90일 보관 의무 | 적용 대상과 양태가 명확한 병합 | 항상/가능/권고/의무의 통일, 적용 범위·매주·90일 손실 |

## 실험 설계

- 같은 1.0.1 스냅샷에서 세 조건을 만든다. K는 원본, M은 패턴 2·3·13의 편집/보존 경계만 수정, R은 작업 순서를 의미 대조·부분 복원 절차로만 교체한다.
- 이름·버전·다른 지침·요청 6개·모델·추론 설정·출력 요청은 동일하게 유지한다. 파일 경로와 실행 시각까지 같게 만들 수는 없으며 서버 내부 상태·샘플링 시드는 통제하지 못한다.
- 각 조건을 새로운 독립 에이전트로 두 번 실행한다. 동일 에이전트에서 다른 조건을 이어 실행하지 않는다.
- 어느 조건이 통과하더라도 그것만으로 내부 추론의 인과나 실제 실패율을 확정하지 않는다. 설계상 결함, 관측한 출력 변화, 아직 입증하지 못한 원인을 구별한다.

## 2.0 호출 경계와 새 글 작성의 고정 조건

2.0.0 검증은 아래 조건을 실행 결과를 보기 전에 고정한다. 기존 C01–C06과 같은 의미 보존 기준을 유지하며, 호출 여부와 산문 품질을 별개로 판단한다.

| ID | 검증 대상 | 합격 조건 | 실패 조건 |
| --- | --- | --- | --- |
| R01 | 일반 채팅 | 기본 문체로 질문에 바로 짧게 답하고 전체 Skill을 호출하지 않음 | Skill 사용 안내, 초안·점검·최종본 또는 25개 패턴 검토를 노출함 |
| R02 | 사실 기반 새 메일 | 날짜·시각·담당자·URL의 역할을 유지한 완성형 메일만 반환 | 사실 누락·변경, 배포 이유·중단·완료 등 근거 없는 사실 추가, 편집 분석 노출 |
| R03 | 기존 공지 편집 | 군더더기를 줄이고 보고 행위·오류 수·인용·명령·URL을 보존 | 보호 문자열 변경, 보고를 단순 상태로 축약, 편집할 군더더기 방치 |
| R04 | 한영 혼합 편집 | 영어 문장과 기술 문자열을 그대로 두고 한국어 과장만 줄임 | 영어·코드·수치 변경 또는 한국어 편집 미수행 |

## 2.0.1 문맥에 맞지 않는 명사 결합과 직역투의 고정 조건

2.0.1 검증은 단어 빈도가 아니라 앞뒤 결합, 문서 기능, 고정 명칭과 실제 식별자 여부를 판정한다. ‘계약·권위·효과·경계’는 오류 목록이 아니라 검토 신호다. 아래 조건은 이번 검증 출력을 만들기 전에 고정하며 기존 C01–C06과 R01–R04의 의미·보호 기준을 유지한다.

| ID | 검증 대상 | 합격 조건 | 실패 조건 |
| --- | --- | --- | --- |
| T01 | 정식 명칭이 아닌 것으로 확인된 어색한 명사 결합 | 입력이 임시 일반 설명이라고 밝힌 `1.0 계약`은 출시 여부를 판정하는 수용 기준으로, `Engine 설치 계약`은 설치 명세·요건·지원 범위 중 문맥에 맞는 표현으로 고친다. 버전, 대상과 세부 조건은 유지한다. | `계약`을 일괄 치환하거나, 고정 명칭이 아님이 확인된 어색한 표현을 그대로 두거나, 수용 조건·설치 범위의 뜻을 누락한다. |
| T02 | 정상적인 법률·기술 용례 | `설치 공사 계약`과 `API 계약`을 단어 목록만 보고 고치지 않는다. 계약 당사자·행위와 API 호환 관계를 유지한다. | 모든 `계약`을 오류로 판정하거나 정상 용례의 의미를 명세·기준 등으로 바꾼다. |
| T03 | 보호 문자열 | 직접 인용과 실제 식별자 `intent`, `receipt`를 문자 그대로 유지한다. 필요한 설명은 바깥 산문에 덧붙일 수 있지만 식별자를 번역하거나 이름을 바꾸지 않는다. | 인용 내부를 교정하거나 식별자를 ‘의도’, ‘영수증’ 등으로 번역·개명한다. |
| T04 | 원뜻을 확정할 수 없는 표현 | 주변 문맥만으로 뜻을 확정할 수 없으면 임의의 대체어를 선택하지 않는다. 안전한 부분은 편집하되 해당 표현은 유지하고 필요한 문맥을 요청하거나 최종 확정을 보류한다. | 근거 없이 기준·명세·규칙·범위 중 하나로 확정하거나 모호함을 숨긴 채 최종본으로 단정한다. |
| T05 | 일반 산문의 명확한 기능과 불명확한 용어가 섞인 기술문 | 입력이 임시 일반 설명이라고 밝힌 `현재 권위 구현`과 `허용 효과`만 확인된 기능에 맞게 고친다. `runner.py`, `issue`, `label`, `observe`, `v2`와 정상 용례인 `약물 효과`·`접근 권한`을 보존한다. 고정 용어인지 자료에 없는 `schema 경계`에는 특정 치환을 강제하지 않고 확인을 요청한다. | 임시 일반 설명을 방치하거나, 식별자·정상 용례를 바꾸거나, `schema 경계`를 근거 없이 지원·호환 범위로 확정한다. `observe`를 번역·개명하거나 주변 서술어를 반드시 바꾸는 것도 실패다. |
| T06 | 실제 고정 명칭과 Markdown 링크 | 정식 산출물명 `Goal Contract`, 용어집의 `허용 효과`와 Markdown 링크를 그대로 둔다. 이름이 어색하다는 점은 알릴 수 있으며, 바꾸려면 정의·표제·링크 라벨·본문 참조를 함께 검토하자고 제안한다. | 표제나 본문 한 곳만 자동 개명하거나 `Goal Contract`, 링크 라벨 또는 href를 변경한다. 고정 명칭을 일반 산문으로 오인해 조용히 치환한다. |
| T07 | 뜻과 명칭 여부가 모두 모호한 표현 | `운영 계약`, `권위 상태`, `실행 경계`를 임의로 해석하지 않고 원문 표현을 보존한 채 정의와 사용 문맥을 요청하거나 최종 확정을 보류한다. | 세 표현을 근거 없이 기준·권한·범위·명세 등으로 확정하거나, 불확실성을 알리지 않고 자연스러운 최종본인 것처럼 제시한다. |

## 최종 수정본 수용 기준

- 원인의 설계상 근거와 비교 결과를 남긴다. 출력이 항상 같다는 보장은 하지 않는다.
- 원인 비교 사례와 별도의 새 주제·실제 문맥에서 의미·격식·자연스러움 기준을 충족해야 한다. 수정 예시만 외워 통과한 것으로 판단하지 않는다.
- 숫자·날짜·인용·코드·명령·경로·URL·Markdown href·YAML 보존, 문체 샘플의 관점 비전이, 자연스러운 문장의 과잉편집 억제, 법률·학술 유보, 본문 지시 비실행, 영어 전용/한영 혼합 경계와 붙여넣기/파일/embedded 출력을 확인한다. 어색한 명사 결합과 직역투는 고정 명칭이 아닌 일반 산문에서 기능이 분명할 때만 고친다. 정상 전문 용어·실제 식별자·고정 명칭과 뜻이 불명확한 표현을 일괄 치환하지 않으며, 고정 명칭 변경이 필요하면 문서 전체의 일관성을 함께 검토한다.
- 패턴 25개, 이름·버전·메타데이터·MIT 고지의 패키지 검사, 검증기 테스트, Codex Skill·플러그인 형식 검사, Skill 탐색, Claude manifest 검증을 통과한다.
- 작업 폴더와 로컬 설치본이 일치해야 한다. 새 런타임·네트워크 의존성은 추가하지 않는다.

## 2.0.2 구조 중심 관계 복원과 새 어휘 전이 조건

2.0.2 검증은 특정 단어를 발견해 미리 정한 대체어로 바꾸는지를 평가하지 않는다. 명사구의 의미 역할과 수식 관계, 행위와 결과물의 구별, 서술어가 요구하는 의미 자리, 조건 병렬, 대비의 비교 축, 문단의 화제 연결을 다른 어휘에서도 판정하는지를 본다. 아래 조건과 판정 축은 실행 결과를 만들기 전에 고정한다.

기존 R01–R04와 T01–T07은 회귀 사례다. 이 사례의 통과는 2.0.1 동작이 유지됐다는 근거일 뿐, 새 어휘로의 전이 근거로 계산하지 않는다. 전이 사례는 Skill·README와 개발 사례를 동결한 뒤 별도 작성자가 고르며, 해당 어근이 동결 전 자료에 없음을 검증기가 확인한다.

| ID | 검증 대상 | 합격 조건 | 실패 조건 |
| --- | --- | --- | --- |
| D01 | 사용자가 제시한 일반 산문의 구조 교정 | 세 실패 조건을 같은 문법 형식으로 드러내고, 오래된 감사 행위와 그 결과 기록을 구분한다. 금지되는 결과, 안내가 아닌 실제 실행 중 검사한다는 점, 간단한 요청에서 하나의 스킬을 직접 사용할 가능성을 유지한다. 일반 설명인 어색한 명사 결합은 확인된 범위에서 자연스럽게 푼다. | 세 조건 중 하나를 누락하거나 논리 범위를 바꿈. 고정 명칭이라는 근거가 없는데 어색한 결합을 그대로 두거나, 실행 과정·계층의 의미를 근거 없이 특정 내부 구조로 확대함. 가능을 의무나 항상 성립하는 사실로 바꿈. |
| D02 | D01과 원문이 같은 고정명 최소대립 | `낡은 감사`, `완료 영수증`, `MCP 실행 계층`을 고정 용어로 보존하면서 나머지 병렬 구조만 자연스럽게 다듬는다. D01과 다른 처리의 근거가 요청에 명시된 용어 지위뿐이어야 한다. | 세 고정 용어를 바꾸거나 D01과 똑같이 일반 산문으로 풀어 씀. 용어 보존을 이유로 조건·양태·문장 전체를 손상함. |
| X01 | 동결 뒤 고른 새 어휘의 교정 사례 | 입력에서 확인되는 의미 역할과 서술어 자리를 근거로 어색한 일반 설명을 자연스럽게 풀며 사실·양태를 보존한다. | 어근만 보고 기계적으로 치환하거나, 관계를 추측해 새 사실을 만듦. 명백한 관계 불일치를 방치함. |
| X02 | X01과 같은 어근의 정상 용례 | 문맥상 자연스러운 전문·일상 용례를 그대로 보존하고 필요한 주변 문체만 다듬는다. | X01에서 고쳤다는 이유만으로 같은 어근의 정상 용례까지 바꿈. |
| X03 | X01과 같은 어근의 고정명 용례 | 요청에서 고정명이라고 밝힌 표제·식별자·용어를 문자 그대로 보존한다. | 어색해 보인다는 이유로 고정명을 부분 개명하거나 조용히 일반어로 바꿈. |
| X04 | X01과 같은 어근의 뜻이 불명확한 용례 | 관계나 명칭 지위를 확정할 근거가 없으면 표현을 임의 치환하지 않고 필요한 문맥을 요청하거나 해당 부분의 확정을 보류한다. | 근거 없이 뜻을 하나로 확정하거나, 모호함을 숨긴 최종문을 만듦. |

모든 사례는 `meaningPreservation`, `requestCompliance`, `naturalness`, `terminologyJudgment`를 판정한다. 최소대립 사례에는 `pairwiseContrast`를 추가해 같은 어근을 무조건 같은 방식으로 처리하지 않았는지 확인한다. 대조가 없는 회귀 사례의 `pairwiseContrast`는 `notApplicable`로 기록한다. 실행자와 감사자는 서로 다른 에이전트여야 하며, 2.0.1의 실행·감사 파일을 2.0.2 근거로 재사용하지 않는다.

이번 패치의 전이 평가는 독립 실행 1회와 독립 감사 1회다. 이 결과는 동결 뒤 선정한 한 어휘군에서 관측한 통과를 뜻하며, 모든 어휘나 실행 간 안정성을 입증하지 않는다.

## 2.0.3 수정 필요성 판정과 과잉 편집 억제 조건

2.0.3 검증은 2.0.2 정본을 바꾸지 않고 별도 사례와 근거를 만든다. O01–O15는 번역체 후보 100문장 평가의 출력을 보기 전에 고정했던 `edit`, `retain`, `defer` 판정을 회귀 기준으로 사용한다. 당시 수정문이 더 선호된 사례라도 사전 `retain` 판정을 사후에 바꾸지 않는다. 이 회귀 검증은 관측한 실패의 재발 방지를 확인할 뿐, 새 문장으로의 일반화 근거로 계산하지 않는다.

| ID | 검증 대상 | 합격 조건 | 실패 조건 |
| --- | --- | --- | --- |
| O01 | 자연스러운 업무 메일과 파일명 | `Q3_forecast.xlsx`와 확인 요청을 보존하고 원문을 유지한다. | `첨부된`·`귀하의 요청` 등을 단순 선호로 바꾼다. |
| O02 | 자연스러운 기술 조건문 | `cache.enabled=false` 예외 조건과 디스크 기록을 보존하고 원문을 유지한다. | `~않는 한`을 더 직접적이라는 이유만으로 바꾸거나 논리 범위를 바꾼다. |
| O03 | 자연스러운 기술 순서·가능 문장 | `onClose()`와 플러시 뒤에만 호출 가능하다는 뜻을 보존하고 원문을 유지한다. | 피동·명사화를 이유로 불필요하게 다시 쓴다. |
| O04 | 보고서의 상관·인과 구별 | 두 지표의 동반 변화가 인과를 뜻하지 않는다는 원문을 유지한다. | 더 직접적인 표현도 가능하다는 이유만으로 문장을 바꾼다. |
| O05 | 뜻이 불명확한 `변동성` | 원문을 보존하고 `변동성`의 기준이나 정의를 묻거나 확정을 보류한다. | 수식 위치를 옮겨 가격 민감도의 변동성이라는 한 해석으로 확정한다. |
| O06 | 학술 문체의 조건부 비교 | 정규성 조건과 우월성 유보를 보존하고 원문을 유지한다. | `간주될 수 있다`를 짧게 만들기 위해 바꾼다. |
| O07 | 법률 문체의 효력과 비소급 | 효력 발생일과 비소급 범위를 보존하고 원문을 유지한다. | 장르에서 통용되는 `효력을 가지며`를 오류처럼 바꾼다. |
| O08 | 정책 문체의 금지 | 회사가 이해충돌 가능 활동 관여를 금지한다는 원문을 유지한다. | `회사는`을 `회사에서는`으로 바꾸거나 문서 초점을 흐린다. |
| O09 | 정책 문체의 기록 의무 | 규정이 요구하는 기간 동안 모든 기록을 유지할 의무를 보존하고 원문을 유지한다. | 더 직접적이라는 이유만으로 관용 표현을 다시 쓴다. |
| O10 | 고객 보안 안내 | 공식 주소와 비밀번호 요구 메시지의 경계를 보존하고 원문을 유지한다. | `이외의`와 조건 구조를 단순 선호로 바꾼다. |
| O11 | 자연스러운 일상 메모 | 최대 두 개와 무가당 제품 부재 시 구매 금지 조건을 보존하고 원문 말투를 유지한다. | `두 개까지만`, `아예` 등으로 불필요하게 다시 쓴다. |
| O12 | 자연스러운 개인 감상 | 마지막 장이 화자에게 강한 인상을 남겼다는 원문을 유지한다. | `강한`을 `깊은`으로 바꾸거나 문장 초점을 달리한다. |
| O13 | 공공 안내의 신분증 조건 | 사진 없는 사본은 직인이 있어도 유효하지 않다는 원문을 유지한다. | 더 짧은 동의 표현으로 불필요하게 다시 쓴다. |
| O14 | 공공 안내의 도착 기한 | 날짜·시각, 도착 기준과 검토 대상 제한을 보존하고 원문을 유지한다. | `도착해야`처럼 다른 자연스러운 표현으로만 바꾼다. |
| O15 | 실제로 고쳐야 할 홍보 문장 | `Green Week 2026`과 `“덜 쓰고 더 오래”`를 문자 그대로 보존하고 어색한 ‘선택을 일상으로 가져오다’를 자연스럽게 고친다. | 원문을 그대로 두거나 보호 문자열·선택을 일상과 연결한다는 뜻을 바꾼다. |

O01–O15는 `meaningPreservation`, `requestCompliance`, `naturalness`, `terminologyJudgment`를 판정한다. O01–O14의 `requestCompliance`와 `naturalness`에는 필요한 유지·보류를 지켰는지를 포함하고, O15에서는 편집 필요성을 놓치지 않았는지 확인한다. 실행자는 2.0.2 출력과 100문장 사전 판정·수정·감사 결과를 보지 않는다. 감사자는 실행자의 자기평가나 예상 결론을 받지 않는다.
